import 'dart:ui';

class Sizes {
  double screenWidth = window.physicalSize.width;
  double screenHeight = window.physicalSize.height;
  double maxMobileDisplayWidth = 700;
  double minScreenWidth = 200;
}
